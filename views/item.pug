extends base

mixin overviewBox(description, text)
    .overview-box__detail

        span.overview-box__label=description
        span.overview-box__text=text
block content


    section.section-header
        .header__hero
            .header__hero-overlay &nbsp;
                img(style="width: 50%; height: auto; position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%);")(src=`/${item.imgPath}` alt="Hero Image")

        .heading-box
            h1.heading-primary
            .heading-box__group
                .heading-box__detail
                    svg.heading-box__icon
                .heading-box__detail
                    svg.heading-box__icon

    section.section-description
        .overview-box
            //div
            //    .overview-box__group
            //        //h2.heading-secondary.ma-bt-lg פרטים נוספים
            //        img(src=`/${item.artistLogo}`)
            //        //+overviewBox("אומנ.ית:",`${item.artist}`)
            //        //+overviewBox("מחיר:", `${item.price}`)
            //        //+overviewBox("",href=`${item.artistPage}`)

            if user
                button.btn#additem(data-item-id=`${item._id}`) לרכישה
            else
                a.btn.btn--green(href='/login') התחבר / הרשם לרכישה

        .description-box
            h2.heading-secondary.ma-bt-lg=`${item.summary}`
            p.description__text=`${item.description}`
